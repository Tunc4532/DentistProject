@model List<ResultSliderCardDto>
@{
    ViewData["Title"] = "SliderCardList";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";
}
<div class="col-xl-12">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">kaydırma kartı Listesi</h4>
            <a href="/Admin/SliderCard/CreateSliderCard/" class="btn btn-success"><i class="fas fa-plus-circle"></i> Yeni kaydırma kartı Girişi</a><br /><br />
            <div class="table-responsive">
                <table class="table table-striped mb-0">
                    <thead>
                        <tr>
                            <th>Başlık1</th>
                            <th>Başlık2</th>
                            <th>Başlık3</th>
                            <th>Sil</th>
                            <th>Güncelle</th>
                        </tr>
                    </thead>
                    @foreach (var item in Model)
                    {
                        <tbody>
                            <tr>
                                <td>@item.Title</td>
                                <td>@item.Title2</td>
                                <td>@item.Title3</td>
                                <td>
                                    <button class="btn btn-danger deleteBtn" data-id="@item.SliderCardId"><i class="fas fa-trash-alt"></i>  Sil</button>
                                </td>
                                <td><a href="/Admin/SliderCard/UpdateSliderCard/@item.SliderCardId" class="btn btn-warning"><i class="fas fa-edit"></i> Güncelle</a></td>
                            </tr>
                        </tbody>
                    }
                </table>
            </div>

        </div>
    </div>
</div>
<script>
    document.addEventListener('DOMContentLoaded', function () {
        setupDeleteConfirmation('.deleteBtn', '/Admin/SliderCard/RemoveSliderCard/');
    });
</script>